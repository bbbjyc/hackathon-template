# 해커톤 협업 가이드

매주 새로운 팀원과 해커톤을 할 때, 첫 30분을 세팅에 낭비하지 않기 위한 가이드입니다.
팀원이 Codex, Cursor, Claude Code 중 무엇을 쓰든 이 문서를 자동으로 읽도록 `CLAUDE.md`와 `.cursorrules`에 이 파일을 가리키는 안내를 넣어뒀습니다. AI 에이전트는 세션 시작 시 반드시 이 문서 전체를 먼저 읽고 작업을 시작하세요.

## 0. 저장소 세팅 (사람이 할 일, 매주 반복)

이 폴더(`hackathon-templates`)를 GitHub에서 **Template Repository**로 등록해두면 매주 이 과정이 클릭 한 번으로 끝납니다.
1. GitHub repo Settings → "Template repository" 체크
2. 매주 시작할 때 "Use this template" 버튼으로 새 repo 생성
3. Settings → Collaborators에서 그 주 팀원 GitHub 아이디 추가 (지난주 팀원은 남겨둘 필요 없음)
4. 클론 후 아래 "1. 프로젝트 스펙" 섹션을 팀 전체가 같이 채우기

## 1. 프로젝트 스펙 (제일 먼저 채우기)

> 이 섹션이 비어있으면 코드부터 짜지 말고 이것부터 채우세요. 여기가 팀 전체의 "계약"입니다. AI에게 맡겨서 자동으로 채우게 하지 말고, 팀이 직접 논의해서 채웁니다.

- **한 줄 목표**: (이 앱이 뭘 해결하는지 한 문장)
- **기술 스택**: (프론트 / 백엔드 / DB / 배포 방식)
- **DB 스키마**: (테이블/필드 초안)
- **API 목록**: (엔드포인트, 요청/응답 형식 — 최소한 표 형태로)
  ```
  POST /api/xxx
  요청: { ... }
  응답: { ... }
  ```
- **환경 변수 / API 키 관리 방식**: (`.env.example` 파일 두고 실제 `.env`는 git에 안 올리기)

## 2. 분업 방식

- **기능(수직) 단위로 나누기**를 기본으로 합니다. 한 사람이 자기 기능의 프론트~백엔드를 통으로 책임집니다.
- 계층(프론트/백엔드)만 나눠서 분업하지 마세요 — 서로 기다리는 병목이 생깁니다. 부득이하게 계층으로 나눠야 하면, 위 "API 목록"부터 완성한 뒤 각자 mock 데이터로 병렬 작업하세요.
- 기능 배정은 이슈트래커/화이트보드에 명시적으로 적어두기 ("A: 로그인", "B: 결제" 등).

## 3. Git / GitHub 규칙

- 브랜치: `feature/기능이름` 형식으로 각자 따로 작업
- **작업 시작 전**: 항상 `git pull` (AI 에이전트도 세션 시작 시 자동으로 실행)
- **작업 끝날 때**: 커밋 + push (작게, 자주)
- 이 문서(`AGENTS.md`)를 수정했다면, 사소한 오타 수정 외에는 팀 단톡방에 "스펙 바뀜" 한 줄 알리고 push하기 — 특히 API/DB 구조를 바꾸는 경우 조용히 push만 하면 다른 사람 코드가 깨집니다.
- PR 없이 main에 바로 push해도 되는 건 문서/스펙 수정 정도. 실제 기능 코드는 최소한 본인 브랜치에서 작업 후 merge하는 걸 권장 (해커톤 시간 제약상 리뷰는 생략해도 되지만, 브랜치 분리는 유지).
- 충돌 나면 당황하지 말고 해당 기능 담당자끼리 짧게 통화/대면으로 해결 (텍스트로 오래 끌지 않기).

## 4. AI(바이브코딩) 활용 시 주의사항

- **모든 AI 에이전트(Codex, Cursor, Claude Code)는 작업 시작 시 이 `AGENTS.md`를 먼저 읽고, `git pull`부터 실행할 것.**
- AI가 만든 코드도 위 "API 목록"과 어긋나지 않는지 반드시 눈으로 확인 후 커밋하세요. AI가 스펙을 완벽히 지킨다는 보장은 없습니다.
- 여러 명이 각자 AI를 쓰면 같은 문제를 다른 방식(다른 라이브러리, 다른 네이밍)으로 풀기 쉽습니다. 애매하면 이 문서에 "이 프로젝트는 OOO 라이브러리만 쓴다" 같은 규칙을 미리 못박아두세요.
- 긴 작업 세션 중 팀원이 이 문서를 업데이트했을 수 있으니, 큰 작업을 시작하기 전엔 다시 한번 `git pull` + 이 문서 재확인.

## 5. 막판 체크리스트 (제출 전)

- [ ] 모든 기능이 위 API 스펙대로 실제로 통신되는지 통합 테스트
- [ ] `.env`, API 키 등 민감정보가 git에 올라가지 않았는지 확인
- [ ] README에 실행 방법 (설치 → 실행 명령어) 정리
