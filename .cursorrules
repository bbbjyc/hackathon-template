# 프로젝트 규칙

이 저장소의 전체 협업 규칙(프로젝트 스펙, 분업 방식, Git 규칙, 체크리스트)은 `AGENTS.md`에 있습니다.

작업을 시작하기 전에 반드시 `AGENTS.md`를 먼저 읽고, 거기 적힌 대로 `git pull`부터 실행하세요.

(참고: 최신 Cursor 버전은 `.cursor/rules/*.mdc` 형식을 쓸 수도 있습니다. 이 프로젝트를 열었을 때 Cursor가 이 파일을 인식하지 못하면, 이 내용을 `.cursor/rules/project.mdc`로 옮겨서 사용하세요.)
